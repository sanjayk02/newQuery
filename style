<Box
  style={{
    width: '100%',
    height: 32,
    backgroundColor: theme.palette.background.paper,
    display: 'flex',
    alignItems: 'center',
    paddingLeft: 16,
    paddingRight: 16,
    marginTop: 4,
    marginBottom: 0,
    borderRadius: 4,
    border: `1px solid ${theme.palette.divider}`,
    boxSizing: 'border-box',
    justifyContent: 'space-between',
  }}
>
  <span style={{ fontSize: 12, fontWeight: 600, lineHeight: '12px' }}>
    Pipeline Phase Priority: {phasePriority.toUpperCase()}
  </span>

  {/* right icon group */}
  <div
    style={{
      display: 'inline-flex',
      alignItems: 'center',
      borderRadius: 4,
      overflow: 'hidden',
      backgroundColor: '#1f1f1f',
    }}
  >
    <IconButton
      size="small"
      onClick={() => setBarView('grid')}
      style={{
        padding: 6,
        borderRadius: 0,
        backgroundColor: barView === 'grid' ? '#2b2b2b' : 'transparent',
      }}
    >
      <ViewModuleIcon style={{ fontSize: 18, color: '#fff' }} />
    </IconButton>

    <IconButton
      size="small"
      onClick={() => setBarView('compact')}
      style={{
        padding: 6,
        borderRadius: 0,
        backgroundColor: barView === 'compact' ? '#2b2b2b' : 'transparent',
      }}
    >
      <ViewComfyIcon style={{ fontSize: 18, color: '#fff' }} />
    </IconButton>

    <IconButton
      size="small"
      onClick={() => setBarView('list')}
      style={{
        padding: 6,
        borderRadius: 0,
        backgroundColor: barView === 'list' ? '#2b2b2b' : 'transparent',
      }}
    >
      <ViewListIcon style={{ fontSize: 18, color: '#00b7ff' }} />
    </IconButton>
  </div>
</Box>
