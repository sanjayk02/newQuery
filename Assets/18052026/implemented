// DEBUG: log what components are being requested
fmt.Printf("[DEBUG ListLatestShotDocuments] param.Component = %v\n", param.Component)

for _, d := range param.Component {
    fmt.Printf("[DEBUG ListLatestShotDocuments] iterating component: %q\n", d)
    if d == "cam_data" {
        fmt.Println("[DEBUG ListLatestShotDocuments] matched 'cam_data', appending projection")
        projectStage[0].Value = append(projectStage[0].Value.(bson.D), bson.E{
            Key:   "cam_data_type",
            Value: "$component_info.cam_data_type",
        })
    }
}

// DEBUG: log the final projectStage so we can see all projections
fmt.Printf("[DEBUG ListLatestShotDocuments] final projectStage = %+v\n", projectStage)
