const allAssets = useMemo(() => {
  if (viewMode === 'grouped') {
    return (groups || []).flatMap((g: any) => g.items || []);
  }
  return assets || rawAssets || [];
}, [viewMode, groups, assets, rawAssets]);
