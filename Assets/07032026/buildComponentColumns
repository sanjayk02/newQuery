const buildComponentColumns = (
  phase: string,
  colors: Colors,
  phaseComponents: Record<string, string[]>
): Column[] =>
  (phaseComponents?.[phase] || []).map((comp) => ({
    id: comp,
    label: `${comp.toUpperCase()} Submitted`,
    colors,
    sortable: true,
    sortKey: comp,
  }));
