{["mdlRend","bldAnm","bldRend","ldvMdl"].includes(col.id) ? (
  <>
    {col.label.split(" ")[0]}
    <br />
    Submitted
  </>
) : col.phase ? (
  <>
    {col.phase.toUpperCase()}
    <br />
    {col.kind ? col.kind.toUpperCase() : ""}
  </>
) : (
  col.label
)}
