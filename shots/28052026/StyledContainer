// Update the styled components for proper scrolling

const StyledContainer = styled(Container)(({ theme }) => ({
    position: 'relative',
    display: 'flex',
    flexDirection: 'column',
    padding: 0,
    height: 'calc(100vh - 64px)',  // Fixed height, adjust 64px to your header height
    overflow: 'hidden',  // Hide overflow on container level
    
    '& > *': {
        display: 'flex',
        overflow: 'hidden',
        padding: theme.spacing(1),
        paddingBottom: 0,
    },
    '& > *:last-child': {
        paddingBottom: theme.spacing(1),
    },
}));

const StyledPaper = styled(Paper)({
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    overflow: 'auto',  // Allow paper to scroll
    flex: 1,  // Take remaining space
    minHeight: 0,  // Important for flex children to scroll
});

const StyledContentDiv = styled('div')(({ theme }) => ({
    backgroundColor: theme.palette.background.paper,
    display: 'flex',
    flexDirection: 'row',
    gap: theme.spacing(2),
    width: '100%',
    minWidth: '100%',
    height: '100%',
    overflow: 'auto',  // Allow horizontal scroll
    flex: 1,
    minHeight: 0,
}));

const StyledTableDiv = styled('div')({
    paddingBottom: 8,
    overflowX: 'auto',  // Horizontal scroll for table
    overflowY: 'auto',  // Vertical scroll for table
    width: '100%',
    height: '100%',
    flex: 1,
    minHeight: 0,  // Critical for flex scrolling
    position: 'relative',
});

const ToolBarWrapper = styled('div')(({ theme }) => ({
    display: 'flex',
    flexDirection: 'column',
    gap: theme.spacing(1),
    flexShrink: 0,  // Toolbar doesn't shrink
}));

const FooterWrapper = styled('div')(({ theme }) => ({
    display: 'flex',
    justifyContent: 'flex-end',
    width: '100%',
    backgroundColor: theme.palette.background.paper,
    paddingRight: theme.spacing(25),
    flexShrink: 0,  // Footer doesn't shrink
}));
